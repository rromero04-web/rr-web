// Both locale layouts declare this image explicitly with their metadataBase.
// A regular route avoids attaching relative image metadata to the global 404.
export { default as GET } from "@/lib/opengraph-image";
export const dynamic = "force-static";
